---
alwaysApply: true
---
## MCP Client Configs (Claude, Cursor, Raycast)

- **Claude Desktop (stdio)** — template lives in [configs/claude-desktop.json](mdc:configs/claude-desktop.json)
  - Command shape: `"command": "uvx", "args": ["--python", "3.13", "--from", ".", "graph-rag-mcp-stdio"], "cwd": repo root, `env` with `GEMINI_API_KEY`, `OBSIDIAN_RAG_VAULTS`.

- **Cursor (HTTP)** — template lives in [configs/cursor-mcp.json](mdc:configs/cursor-mcp.json)
  - Start server: `uv run graph-rag-mcp-http` (default `http://localhost:8765`).
  - Point Cursor MCP extension to that URL.

- **Raycast (HTTP)** — template lives in [configs/raycast-config.json](mdc:configs/raycast-config.json)
  - Start: `uv run graph-rag-mcp-http`; install the generated extension per template.

- See [README.md](mdc:README.md#🛠️-available-commands) and [SETUP.md](mdc:SETUP.md) for details.
